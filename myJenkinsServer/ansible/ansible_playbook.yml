---
- name: Install Latest Jenkins on Ubuntu
  hosts: all
  become: true
  vars:
    java_packages:
      - fontconfig
      - openjdk-21-jre
    utils_packages:
      - apt-transport-https
      - wget
      - curl
      - gnupg
      - software-properties-common
      - git
      - xmlstarlet
      - cron

  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes
        cache_valid_time: 3600

    - name: Install Java packages
      apt:
        name: "{{ java_packages }}"
        state: present

    - name: Install utility packages
      apt:
        name: "{{ utils_packages }}"
        state: present

    - name: Add Jenkins apt repository key
      get_url:
        url: https://pkg.jenkins.io/debian-stable/jenkins.io-2023.key
        dest: /usr/share/keyrings/jenkins-keyring.asc
        mode: '0644'

    - name: Add Jenkins apt repository
      apt_repository:
        repo: "deb [signed-by=/usr/share/keyrings/jenkins-keyring.asc] https://pkg.jenkins.io/debian-stable binary/"
        filename: jenkins
        state: present

    - name: Update apt cache after adding Jenkins repo
      apt:
        update_cache: yes

    - name: Install Jenkins
      apt:
        name: jenkins
        state: present

    - name: Enable and start Jenkins service
      systemd:
        name: jenkins
        enabled: yes
        state: started
        daemon_reload: yes

    - name: Allow Jenkins user to sudo without password
      lineinfile:
        path: /etc/sudoers.d/jenkins
        create: yes
        mode: '0440'
        line: 'jenkins ALL=(ALL) NOPASSWD: ALL'
        validate: '/usr/sbin/visudo -cf %s'

    - name: Create a script to update Jenkins public IP at boot
      copy:
        dest: "/usr/local/bin/update_jenkins_ip.sh"
        mode: '0755'
        content: |
          #!/bin/bash
          IP=$(curl -s http://169.254.169.254/latest/meta-data/public-ipv4)
          CONFIG_FILE="/var/lib/jenkins/jenkins.model.JenkinsLocationConfiguration.xml"
          if [ -f "$CONFIG_FILE" ]; then
            xmlstarlet ed --inplace -u "/jenkins.model.JenkinsLocationConfiguration/jenkinsUrl" -v "http://${IP}:8080/" "$CONFIG_FILE"
            systemctl restart jenkins
          fi

    - name: Add script to crontab to update IP on reboot
      cron:
        name: "Update Jenkins public IP at reboot"
        special_time: reboot
        job: "/usr/local/bin/update_jenkins_ip.sh"


